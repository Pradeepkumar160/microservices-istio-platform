apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: production
type: Opaque
stringData:
  # Change this to a strong random value in production!
  # Generate with: openssl rand -base64 64
  secret: "supersecret-change-in-production-use-openssl-rand-base64-64"
